import React from 'react';
import { Image } from 'react-native';

const RnImage = ({ source }) => {
  return <Image source={source} style={{ width: 305, height: 159 }} />;
};

export default RnImage;
